#

## Evaluation

* Extend binary functions from + to +, -, *, /
* Only support integers (to make Rust easier)
* Change run to return a string
* Common (integration) tests based on test_definitions.csv, unit tests in source files
